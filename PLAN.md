## Current Status

None.

## Active Task

None.

## Backlog

- Establish the foundational Vite, React, and testing infrastructure; tests addedn as `__test__` dir in parent dir (i.e., `/atoms/__tests__/Button.test.tsx`)

- Initialize the local IndexedDB database structure and seed the primary store data.

- Connect the application's data-fetching layer to the local database for seamless orchestration.

- Build the responsive application shell and core atomic UI components.

- Integrate local machine learning models to enable in-browser semantic aisle matching.

- Implement complete capabilities for creating and managing the standing default grocery list.

- Create the workflow to generate a specific weekly shopping list from defaults and one-off additions.

- Build the active, chronologically sorted in-store shopping view.

- Configure the progressive web app settings for full offline installation and asset caching.

- Finalize the application with end-to-end testing across the primary user workflows.
