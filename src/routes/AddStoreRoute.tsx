import StoreImporter from '@/components/organisms/StoreImporter';

/** The `/stores/new` screen — add a user-authored store from JSON (ADR-0024). */
export default function AddStoreRoute() {
  return <StoreImporter />;
}
